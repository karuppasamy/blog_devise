<%= form_for @comment do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :post_id %><br />
    <%= f.number_field :post_id %>
  </p>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p><%= f.submit %></p>
<% end %>
